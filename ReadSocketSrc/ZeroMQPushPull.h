#ifndef ZEROMQPUSHPULL_H
#define ZEROMQPUSHPULL_H

#include <list>
#include <thread>
/*
using std::list;
using std::string;
using std::thread;

class ZeroMQPushPull
{
private:
	int nodeId;
	int numNodes;
	long hwm; //high-water mark
	thread pullThread;

public:
	ZeroMQPushPull(int numNodes,
			       int nodeId,
				   list<string> hostnames,
				   list<int> ports,
				   long hwm);

private:
	void pull();




};
*/
#endif
